## 2024-04-08 - Added ARIA labels to icon-only buttons
**Learning:** The application has several icon-only buttons that are missing `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Add `aria-label` attributes to all icon-only buttons to improve accessibility.
